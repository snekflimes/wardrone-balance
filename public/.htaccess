DirectoryIndex index.html

# Some panels add global rewrite rules for the main site (e.g. to index.php).
# This subfolder is a static Vite build; disable rewrites here to avoid blank pages.
<IfModule mod_rewrite.c>
  RewriteEngine Off
</IfModule>

